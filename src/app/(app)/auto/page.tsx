import Link from "next/link";import {redirect} from "next/navigation";import {AutoControls} from "@/components/auto-controls";import {PageHeading} from "@/components/page-heading";import {getAutoOverview} from "@/features/auto/services";import {createVideoCostPlan} from "@/features/video/provider-routing";import {serverEnv} from "@/lib/server-env";import {createClient} from "@/lib/supabase/server";
export default async function AutoPage(){const client=await createClient(),{data}=await client.auth.getUser();if(!data.user)redirect("/login");const overview=await getAutoOverview(client,data.user.id),s=overview.summary,pilot=createVideoCostPlan("CURRENT_3_ACCOUNTS"),lite=pilot.providers.find(x=>x.provider==="google-veo-lite")!;return <><PageHeading eyebrow="FULL AUTO MODE" title="Auto Orchestration" description="วางแผนทุกบัญชีแบบ checkpointed, approval-aware และ fail-closed โดยปิด real publishing และ paid provider ไว้" action={<AutoControls runId={overview.activeRun?.id} state={overview.activeRun?.state}/>}/><section className="stats-grid">{[["Running",s.running],["Paused",s.paused],["Blocked",s.blocked],["Waiting approval",s.waitingApprovals],["Waiting slot",s.waitingSlots],["Cost today",`$${s.costToday.toFixed(2)}`],["Generated",s.generatedToday],["Published",s.publishedToday]].map(([k,v])=><article className="stat-card violet" key={k}><p>{k}</p><strong>{v}</strong><small>Phase 10 local/mock boundary</small></article>)}</section><section className="detail-summary-grid"><article className="panel"><div className="panel-heading"><div><p className="eyebrow">VIDEO PROVIDER</p><h2>Google Veo 3.1 Lite</h2></div><span className={`status-badge ${serverEnv.googleGenAIApiKey?"ready":"blocked"}`}>{serverEnv.googleGenAIApiKey?"CONFIGURED":"WAITING_FOR_PROVIDER"}</span></div><dl className="detail-list"><div><dt>Model</dt><dd>{serverEnv.googleVeoModel}</dd></div><div><dt>Paid mode</dt><dd>DISABLED</dd></div><div><dt>Benchmark</dt><dd>PLANNED</dd></div><div><dt>Retry policy</dt><dd>Bounded; no blind generation resubmit</dd></div></dl></article><article className="panel"><div className="panel-heading"><div><p className="eyebrow">3-ACCOUNT PLAN</p><h2>{pilot.mastersPerDay.join("–")} masters → {pilot.finalsPerDay.join("–")} finals/day</h2></div><span className="phase-chip">LOCAL VARIATIONS</span></div><dl className="detail-list"><div><dt>Lite nominal/month</dt><dd>${lite.nominalMonthlyUsd.join("–")}</dd></div><div><dt>Retry-adjusted/month</dt><dd>${lite.retryAdjustedMonthlyUsd.join("–")}</dd></div><div><dt>Manual Flow</dt><dd>REFERENCE ONLY</dd></div></dl></article></section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">RUN HISTORY</p><h2>Durable runs</h2></div><span className="phase-chip">full-auto-mode-v1</span></div>{overview.runs.length?<div className="data-list">{overview.runs.map(run=><div key={run.id}><Link href={`/auto/runs/${run.id}`}><strong>{run.run_date} · {run.state}</strong></Link><span>${Number(run.spent_usd).toFixed(2)} / ${Number(run.budget_usd).toFixed(2)}</span><small>{run.current_step} · attempt {run.attempt} · {run.blockers_json.join(", ")||"ไม่มี blocker"}</small></div>)}</div>:<p className="muted">ยังไม่มี Auto run — START จะสร้างแผนและหยุดที่ approval โดยไม่ส่งงานจริงออกภายนอก</p>}</section></>}
