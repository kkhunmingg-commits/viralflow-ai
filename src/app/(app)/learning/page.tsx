import {redirect} from "next/navigation";import {PageHeading} from "@/components/page-heading";import {getLearningOverview} from "@/features/analytics/services";import {createClient} from "@/lib/supabase/server";
export default async function LearningPage(){const client=await createClient();const {data}=await client.auth.getUser();if(!data.user)redirect("/login");const {signals,decisions,experiments}=await getLearningOverview(client,data.user.id);return <><PageHeading eyebrow="CONTROLLED LEARNING" title="Learning Loop" description="สัญญาณเฉพาะบัญชีมี confidence และ time decay; การปรับสมองเดิมถูกจำกัดและตรวจย้อนหลังได้"/><section className="stats-grid">{[["Signals",signals.length],["Decisions",decisions.length],["Experiments",experiments.length]].map(([k,v])=><article className="stat-card violet" key={k}><p>{k}</p><strong>{v}</strong><small>append-only evidence</small></article>)}</section><section className="panel"><div className="panel-heading"><div><p className="eyebrow">LEARNING SIGNALS</p><h2>สิ่งที่ระบบเรียนรู้</h2></div><span className="phase-chip">learning-loop-v1</span></div>{signals.length?<div className="radar-table-wrap"><table className="radar-table"><thead><tr><th>Dimension</th><th>Value</th><th>Effect</th><th>Weight</th><th>Confidence</th><th>Decay</th></tr></thead><tbody>{signals.map(s=><tr key={s.id}><td>{s.dimension_type}</td><td>{s.dimension_value}</td><td>{Number(s.effect).toFixed(2)}</td><td>{Number(s.weight).toFixed(2)}</td><td>{Math.round(Number(s.confidence)*100)}%</td><td>{Number(s.decay_factor).toFixed(2)}</td></tr>)}</tbody></table></div>:<div className="large-empty"><span className="empty-orbit">LL</span><h2>ยังไม่มีสัญญาณเรียนรู้</h2><p>ระบบจะเรียนรู้หลังมีข้อมูลจริงเพียงพอ โดยไม่เปลี่ยนสูตรเดิมย้อนหลัง</p></div>}</section></>}
